[project]
name = "fct"
version = "0.1.0"
description = "FastAPI service for Google AI-backed scam signal analysis"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "python-dotenv>=1.0.1",
    "fastapi>=0.139.0",
    "httpx>=0.28.1",
    "uvicorn[standard]>=0.30.0",
]
